import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Worklance.AI - AI-First Service Solutions',
  description: 'Transform your business with our cutting-edge AI-first services. Expert AI solutions for modern enterprises.',
  keywords: 'AI services, artificial intelligence, machine learning, business automation, AI consulting',
  authors: [{ name: 'Worklance.AI Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
